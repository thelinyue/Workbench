## v1.2.5

- 分析中心改为展示完整日志生命周期，并整合存储占用与手动到期清理入口。
- 报告统一生成到实际解压目录下的 `report` 子目录，便于和原始解压内容一起核查。
- 停止跨启动恢复报告标签，避免重启后打开过期或已移动的报告路径。
- 调整安装包与系统显示名称为 `Hephaestus工作台`。

系统要求：Windows 10/11 x64。

安装包未使用代码签名证书，Windows 可能显示未知发布者提示。请使用 `SHA256SUMS.txt` 校验安装包完整性。
