## v1.2.12

- 普通模式和维护者模式统一采用“先创建分类，再创建规则”的流程。
- 新增独立分类管理和分类校验，允许先保存空分类。
- 规则编辑器升级至 v1.2.5，支持分类先建后用及旧数据兼容迁移。

验证：`go test ./...`；`go vet ./...`；`dotnet test tests/HephaestusWorkbench.Tests/HephaestusWorkbench.Tests.csproj -c Release --no-restore`。
