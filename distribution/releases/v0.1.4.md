## v0.1.4

- 修复应用中心下载 GitHub Release 应用包时未跟随资源重定向，导致安装提示 `fetch failed` 的问题。
- 同步 LVM 缓存清理工具已发布 ZIP 的哈希和签名元数据，恢复完整性校验后的安装流程。
- 修复 Windows CI 中终端分发构建测试超时和换行符相关的脆弱断言。
- 禁止 electron-builder 在标签构建期间隐式发布，由发布工作流显式创建并复核 GitHub Release。

验证：`npm run typecheck`；`npm test`；`npm run package:win`。
