## v1.2.9

- 增加隐藏的维护者模式入口，支持维护者密钥校验和连续失败锁定。
- 增加维护者首次配置向导，仓库配置使用当前 Windows 用户的 DPAPI 加密保存。
- 增加 GitHub Fine-grained Token 的内存态使用方式，退出维护者模式后清除。
- 支持维护者编辑主规则草稿并通过 GitHub 分支和 Pull Request 提交审核。
- 更新规则编辑器 Web 插件，普通用户界面不显示维护者入口。

验证：`dotnet test HephaestusWorkbench.sln -c Release --no-restore`。
