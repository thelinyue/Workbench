## v1.2.3

- GitHub Actions 从官方插件 Releases 仓库自动获取最高正式版本的日志分析插件。
- 源码仓库不再依赖或携带日志分析插件二进制文件。
- 普通源码构建不需要插件二进制；正式安装包仍会将 CI 下载并校验后的插件注入 `PluginSeed`。

建议所有 v1.2.2 及更早版本用户升级。本版本可直接覆盖安装，用户数据会保留。

系统要求：Windows 10/11 x64。

安装包尚未使用代码签名证书，Windows 可能显示未知发布者。请使用 `SHA256SUMS.txt` 校验完整性。
