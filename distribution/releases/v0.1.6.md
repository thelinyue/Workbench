# v0.1.6

- 分析中心支持以原生独立窗口启动。
- 恢复独立窗口的尺寸和最大化状态。
- 工作台退出时优雅停止后台任务。

## 发布前置条件

运行 `package:win` 前必须依次完成：将现有已签名 Terminal 1.0.1 元数据回填为统一资产名 `terminal-v1.0.1.release.json`，发布已签名 Analysis Center 2.0.0，再构建 Workbench 0.1.6。
